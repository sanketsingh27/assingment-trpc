import Layout from "~/components/Layout";
import OTPcomponent from "~/components/OTP";

const OTP: React.FC = () => {
  return (
    <Layout>
      <OTPcomponent />
    </Layout>
  );
};

export default OTP;
